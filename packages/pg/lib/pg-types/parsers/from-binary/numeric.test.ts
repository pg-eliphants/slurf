import { loadData } from '@test-helpers';
import readNumeric from './numeric';

readNumeric;
loadData;

describe('pg-dates', function () {
    it('test decode bin -> js number string', () => {
        expect(false).toBe(false);
    });
});
